import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LovedOnez',
  webDir: 'http://lovedOnez.etopick.com',
  bundledWebRuntime: false
};

export default config;
